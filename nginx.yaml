- name: nginx
  hosts: mongodb
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.yum:
      name: nginx
      state: install
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started
        